# dirsql

Negation, name interpolation, and input-dependent callees.

- **D10** — A negated glob excludes an otherwise-matching file.
- **J6** — `name:` interpolating a static matrix value.
- **R4** — The callee's job set depends on the caller's `with:` inputs.

No pull request identified yet (#255); the capture lands in a `<pr>/` directory here.
