# template-lib

The canonical fleet shape: mixed globs, two callees, a skipped job.

- **D12** — An extension globstar beside a bare path in one list.
- **J7** — Job-level `if: false` — GitHub still emits a skipped check.
- **R2** — Two distinct callee repos in one workflow.
- **R3** — One callee, several caller jobs, one file — three name prefixes.

No pull request identified yet (#255); the capture lands in a `<pr>/` directory here.
