# curtaincall

Path filtering both ways, plus the common matrix shape.

- **D7** — `paths:` with a matching file.
- **D8** — `paths:` with no matching file — the dominant reason a check is absent.
- **J3** — Single-axis static matrix.

No pull request identified yet (#255); the capture lands in a `<pr>/` directory here.
