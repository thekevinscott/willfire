# lamp-the-djinn

Base-ref gating in both directions.

- **D5** — `branches:` matches the base ref.
- **D6** — `branches:` does not match — a PR into a base other than main.
- **J5** — `include:` with no base axis — rows come from include alone.

No pull request identified yet (#255); the capture lands in a `<pr>/` directory here.
