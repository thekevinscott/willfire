# pr-monitor

A pull request whose merge commit exists and is read.

- **P1** — A merge commit exists — read the tree there, not at head.

No pull request identified yet (#255); the capture lands in a `<pr>/` directory here.
