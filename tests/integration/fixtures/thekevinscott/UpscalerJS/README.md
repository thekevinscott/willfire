# UpscalerJS

A repo with no pull request trigger predicts an empty set.

- **D1** — No `pull_request` trigger — push, schedule or dispatch only.

No pull request identified yet (#255); the capture lands in a `<pr>/` directory here.
