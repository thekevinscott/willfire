["Build","Build + smoke","Build the CLI from HEAD (hermetic)","CI Gate","Colocated test (packages/node/src) / CHANGELOG + MIGRATIONS touched","Colocated test (packages/node/src) / Detect requested languages with sources","Colocated test (packages/node/src) / E2E attestation freshness (e2e verify)","Colocated test (packages/node/src) / Packaging (no test files in the built artifact)","Colocated test (packages/node/src) / Static checks (typescript)","Colocated test (packages/node/src) / Unit mutation — changed lines (${{ matrix.language }})","Colocated test (packages/node/src) / Unit-test coverage (${{ matrix.language }})","Colocated test (packages/node/src) / Unit-test coverage — changed lines (${{ matrix.language }})","Deploy","E2E attestation (packages/node) / CHANGELOG + MIGRATIONS touched","E2E attestation (packages/node) / Detect requested languages with sources","E2E attestation (packages/node) / E2E attestation freshness (e2e verify)","E2E attestation (packages/node) / Packaging (no test files in the built artifact)","E2E attestation (packages/node) / Static checks (${{ matrix.language }})","E2E attestation (packages/node) / Unit mutation — changed lines (${{ matrix.language }})","E2E attestation (packages/node) / Unit-test coverage (${{ matrix.language }})","E2E attestation (packages/node) / Unit-test coverage — changed lines (${{ matrix.language }})","Integration + e2e tests + coverage (100%)","Lint","Lint (fmt + clippy)","Shipped reusable workflow over the Python plugin source / CHANGELOG + MIGRATIONS touched","Shipped reusable workflow over the Python plugin source / Detect requested languages with sources","Shipped reusable workflow over the Python plugin source / E2E attestation freshness (e2e verify)","Shipped reusable workflow over the Python plugin source / Packaging (no test files in the built artifact)","Shipped reusable workflow over the Python plugin source / Static checks (python)","Shipped reusable workflow over the Python plugin source / Unit mutation — changed lines (python)","Shipped reusable workflow over the Python plugin source / Unit-test coverage (python)","Shipped reusable workflow over the Python plugin source / Unit-test coverage — changed lines (python)","Shipped reusable workflow over the detect action's package / CHANGELOG + MIGRATIONS touched","Shipped reusable workflow over the detect action's package / Detect requested languages with sources","Shipped reusable workflow over the detect action's package / E2E attestation freshness (e2e verify)","Shipped reusable workflow over the detect action's package / Packaging (no test files in the built artifact)","Shipped reusable workflow over the detect action's package / Static checks (python)","Shipped reusable workflow over the detect action's package / Unit mutation — changed lines (python)","Shipped reusable workflow over the detect action's package / Unit-test coverage (python)","Shipped reusable workflow over the detect action's package / Unit-test coverage — changed lines (python)","Shipped reusable workflow over the move-major-tag helper / CHANGELOG + MIGRATIONS touched","Shipped reusable workflow over the move-major-tag helper / Detect requested languages with sources","Shipped reusable workflow over the move-major-tag helper / E2E attestation freshness (e2e verify)","Shipped reusable workflow over the move-major-tag helper / Packaging (no test files in the built artifact)","Shipped reusable workflow over the move-major-tag helper / Static checks (python)","Shipped reusable workflow over the move-major-tag helper / Unit mutation — changed lines (python)","Shipped reusable workflow over the move-major-tag helper / Unit-test coverage (python)","Shipped reusable workflow over the move-major-tag helper / Unit-test coverage — changed lines (python)","Shipped reusable workflow over the self-test checks package / CHANGELOG + MIGRATIONS touched","Shipped reusable workflow over the self-test checks package / Detect requested languages with sources","Shipped reusable workflow over the self-test checks package / E2E attestation freshness (e2e verify)","Shipped reusable workflow over the self-test checks package / Packaging (no test files in the built artifact)","Shipped reusable workflow over the self-test checks package / Static checks (python)","Shipped reusable workflow over the self-test checks package / Unit mutation — changed lines (python)","Shipped reusable workflow over the self-test checks package / Unit-test coverage (python)","Shipped reusable workflow over the self-test checks package / Unit-test coverage — changed lines (python)","Test","Typecheck","Unit lint (packages/node/src) / CHANGELOG + MIGRATIONS touched","Unit lint (packages/node/src) / Detect requested languages with sources","Unit lint (packages/node/src) / E2E attestation freshness (e2e verify)","Unit lint (packages/node/src) / Packaging (no test files in the built artifact)","Unit lint (packages/node/src) / Static checks (typescript)","Unit lint (packages/node/src) / Unit mutation — changed lines (${{ matrix.language }})","Unit lint (packages/node/src) / Unit-test coverage (${{ matrix.language }})","Unit lint (packages/node/src) / Unit-test coverage — changed lines (${{ matrix.language }})","Unit tests","Windows checkout path budget","putitoutthere check","tc-checks — unit + e2e"]
