# repo-name-checker

The fleet's only `paths-ignore`, and a cartesian matrix.

- **D9** — `paths-ignore:` covers every changed file.
- **J4** — Two-axis static matrix — the full cartesian product.

No pull request identified yet (#255); the capture lands in a `<pr>/` directory here.
