# steervec

An action-gated check declining, and a dependent skipping with it.

- **D4** — `types:` excludes the PR's action — an edited-gated check on a synchronize.
- **J8** — `needs:` on a job that will be skipped.

No pull request identified yet (#255); the capture lands in a `<pr>/` directory here.
