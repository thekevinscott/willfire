# repo-name-checker

The only `paths-ignore` in the fleet, plus a two-axis matrix. No reusable workflows, so nothing here can drift on its own.

No pull request identified yet (#255); the capture lands in a `<pr>/` directory here.
