# putitoutthere

The runtime fan-out. Asserts the decidable entries still match and the undecidable ones are still reported `unknown` rather than guessed.

No pull request identified yet (#255); the capture lands in a `<pr>/` directory here.
